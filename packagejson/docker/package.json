{
  "description": "these scripts will likely need `$ sudo npm run _____` invocation",
	"scripts": {
    "":""
    , "docker.build": "docker build -t jcmoore/nodebb-bubble:0.0.1-gae ../.."
    , "docker.run": "docker run --rm --name nodebb -it -p 8080:8080 -v $PWD/../../../nodebb.config.json:/app/vendor/nodebb/config.backup.json jcmoore/nodebb-bubble:0.0.1-gae"
    , "docker.exec": "docker exec -it nodebb"
	}
}
